// 4.10.3 집합
// 집합 (set)은 중복되지 않는 요소들로 구성된 자료구조이다.
// setOf() 메서드로 읽기 전용 집합을, mutablesotOf() 메서드로 수정 가능한 집합을 생성한다.
// 집합은 이 챍의 예제에서는 사용하지 않지만 리스트, 맵과 함께 대표적인 기본 자료구조이므로
// 알아두면 좋다.

// 읽기 전용 집합
val citySet = setOf("서울", " 수원", "부산")

// 수정가능한 집합

val citySet2 = mutableSetOf("서울", "수원", "부산")

citySet2.add("안양") // [서울, 수원, 부산, 안양]
citySet2.remove("수원") // [서울, 부산, 안양]

//집합의 크기
println(citySet2.size) // 3
// '서울'이 집합네 포함되었는지
println(citySet2.contains("서울")) // true
