//4.9.6 엘비스 연산자(?:)
// 안전한 호츨 시 null이 아닌 기본값을 반환하고 싶을 때는 엘비스 연산자를 함께 사용한다.
// 마지막 코즈는 null값이 아닌 "초기화 하시오"라는 문자열을 반환합니다.

val str : String? = null

var upperCase = if (str != null) str else null // null

upperCase = str?.toUpperCase() ?: "초기화하시오" // 초기화하시오
